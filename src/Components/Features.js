
import "../css/features.css";

export default function Features(props){
    return (
        <div className={props.darkMode ? "dark" : ""}>
            
           Features page
        </div>

        
    )
}