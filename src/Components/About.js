
import "../css/about.css";

export default function About(props){
    return (
        <div className={props.darkMode ? "dark" : ""}>
            
           About page
        </div>

        
    )
}