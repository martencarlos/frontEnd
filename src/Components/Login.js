
import "../css/login.css";

export default function Login(props){
    return (
        <div className={props.darkMode ? "dark" : ""}>
            
           login page
        </div>

        
    )
}