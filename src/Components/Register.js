
import "../css/register.css";

export default function Register(props){
    return (
        <div className={props.darkMode ? "dark" : ""}>
            
           Register page
        </div>

        
    )
}